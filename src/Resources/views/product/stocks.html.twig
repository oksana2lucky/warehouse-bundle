{% extends '@Oksana2luckyWarehouse/base.html.twig' %}

{% block title %}Warehouse | Product Stocks List{% endblock %}

{% block body %}
    <div class="container">
        <h2>Product Stocks List</h2>

        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Quantity</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>
                {% for stock in stocks %}
                    <tr>
                        <td>{{ stock.id }}</td>
                        <td><a href="{{ path('oksana2lucky_warehouse_stock_view', {'id': stock.id}) }}">{{ stock.name }}</a></td>
                        <td>{{ stock.address }}</a></td>
                        <td><a href="{{ path('oksana2lucky_warehouse_product_removeStock', {'id': product.id, 'stockId': stock.id}) }}">Remove</a></td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">no records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}